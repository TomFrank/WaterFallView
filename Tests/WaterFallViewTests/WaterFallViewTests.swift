import XCTest
@testable import WaterFallView

final class WaterFallViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WaterFallView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
